//Create Event
orbit_target=instance_nearest(x, y, oGuard); // adapt to the name of your player object's name
orbit_speed=8; // how fast the orbiting object travels within it's orbit
orbit_length=40; // how big the orbit circunference is
orbit_place=0; // starting position of the object - in this case at the right of the player (not much relevant as it will change by orbiting in the steps)